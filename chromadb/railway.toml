# ChromaDB Railway Configuration
[build]
dockerfile = "Dockerfile"

[deploy]
startCommand = "chroma run --host 0.0.0.0 --port 8000 --path /chroma/chroma"
healthcheckPath = "/api/v1/heartbeat"
healthcheckTimeout = 300
restartPolicy = "on_failure"
